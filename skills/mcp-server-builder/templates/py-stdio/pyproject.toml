[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-mcp-server"
version = "0.1.0"
description = "An MCP server"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
]

[project.scripts]
my-mcp-server = "server:main"
